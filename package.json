{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^5.2.0"
  },
  "scripts": {
    "build": "yarn workspace react-custom-swipe publish:npm",
    "postbuild": "yarn workspace react-custom-swipe publish:type",
    "publish:swipe": "yarn workspace react-custom-swipe publish"
  }
}
