{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^5.2.0"
  },
  "scripts": {
    "reactbuild": "yarn workspace react-custom-swipe publish:npm",
    "postreactbuild": "yarn workspace react-custom-swipe publish:type",
    "reactpublish:swipe": "yarn workspace react-custom-swipe publish"
  }
}
